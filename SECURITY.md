# 安全说明 Security

## 隐私承诺

**本项目不收集、不存储、不上传任何用户数据。**

- 无统计埋点、无广告 SDK、无第三方跟踪
- 应用运行期间产生的唯一持久数据是本地设置（方向字位置、间距、字号、颜色等），全部保存在手机本地的 SharedPreferences，不离开你的设备

## 联网行为

应用唯一的网络请求是**启动时静默查询一次版本更新信息**（GET `version.json`），用于提示是否有新版本：

- 请求内容仅为标准的 HTTP GET，不携带任何设备信息、位置、账号或使用行为
- 该请求为只读查询，服务器不会收到你的任何个人数据
- 不联网不影响应用核心功能（悬浮窗、拖拽、设置全部离线可用）

## 权限

| 权限 | 用途 | 是否必需 |
|------|------|----------|
| 显示在其他应用上层（SYSTEM_ALERT_WINDOW） | 悬浮窗常驻显示 | 是（核心功能） |
| 前台服务（FOREGROUND_SERVICE） | 维持悬浮窗后台存活 | 是 |
| 网络（INTERNET） | 启动时一次版本检查 | 否（无网络也能用） |

## 风险告知

本工具提供的是静态方位文字叠加层，属于屏幕覆盖类应用。部分联机游戏的反作弊系统会检测悬浮窗 / 屏幕覆盖层，使用本工具可能导致账号风险。请仅在**单机游戏**或个人场景使用，后果由使用者自行评估承担。

## 安全报告

如果你发现本项目存在安全问题（例如代码漏洞、隐私泄露隐患），请通过 [GitHub Issues](https://github.com/kkxxdbx/compass-overlay/issues) 提交，并选择 `security` 标签，我会优先处理。
